import ContactSection from "@/components/ContactSection";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Alex Reyes | Contact",
};

export default function ContactPage() {
  return <ContactSection />;
}
