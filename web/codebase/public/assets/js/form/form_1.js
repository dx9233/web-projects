document.addEventListener('DOMContentLoaded', () => {
  const quizButton = document.querySelector('.quiz-start');

  quizButton.addEventListener('click', () => {
      alert('Квиз будет доступен в следующей версии!');
  });
});
