---
title: "Git отказывается принимать пуш"
category: "Git"
---

## 🧩 Проблема

GitLab (или GitHub) отказывался принимать пуш:

```
remote: GitLab: LFS objects are missing. Ensure LFS is properly set up or try a manual "git lfs push --all".
! [remote rejected] main -> main (pre-receive hook declined)
```

При этом команда `git lfs ls-files` показывала список файлов, хранящихся в LFS (обычно это большие изображения, видео или бинарники).
Git требовал загрузить эти объекты через `git lfs push --all`, но ты хотел **навсегда избавиться от LFS**, а не чинить его.

---

## ⚙️ Цель

Полностью удалить LFS из репозитория и вернуть все файлы в обычные Git-объекты,
чтобы пушить, пуллить и деплоить без LFS.

---

## 🚀 Финальное решение (рабочая схема)

Ниже — вся последовательность команд, которая в итоге **реально сработала**:

### 1. Проверить, что LFS действительно включён

```bash
git lfs ls-files
```

Если вывод показывает файлы (PNG, SVG и т.п.), значит они хранятся в LFS.

---

### 2. Создать временный текстовый файл для очистки

Этот трюк говорит `git filter-repo`, что нужно **удалить LFS-заголовки** из истории:

```bash
echo "version https://git-lfs.github.com/spec/v1==>" > lfs-clean.txt
```

---

### 3. Прогнать `git filter-repo`, чтобы удалить все следы LFS

```bash
git filter-repo --force --replace-text lfs-clean.txt
```

Что делает эта команда:

- проходит по всей истории коммитов,
- заменяет текст `version https://git-lfs.github.com/spec/v1` (то есть следы LFS),
- переписывает репозиторий с чистыми бинарниками.

> 💡 Примечание: после выполнения Git сам удалит remote `origin`, чтобы ты случайно не затёр старый репозиторий с LFS.

---

### 4. Удалить локальные данные LFS и вычистить конфиг

```bash
rm -rf .git/lfs
git lfs uninstall
git lfs ls-files
```

После этого `git lfs ls-files` должен вернуть **пустой результат**.

---

### 5. Заново добавить remote

Так как `filter-repo` удалил старый `origin`, нужно прописать новый.

Если ты хочешь запушить в **новый чистый репозиторий** (это безопаснее):

```bash
git remote add origin https://gitlab.com/<твой_юзернейм>/<новый_репо>.git
```

---

### 6. Силой запушить чистую историю

```bash
git push -u origin main --force
```

Git создаст чистую ветку без LFS.
Если репозиторий пуст — он создастся автоматически, и в консоли появится подтверждение:

```
remote: The private project ... was successfully created.
To https://gitlab.com/...git
 * [new branch]      main -> main
```

---

### 7. Проверить, что всё работает

Теперь:

```bash
git lfs ls-files   # → ничего не выводит
git push origin main  # → пуш проходит без ошибок
```

Ты можешь спокойно деплоить проект — без LFS, без зависимостей, без подвохов.

---

## 🧹 Резюме

| Этап                  | Команда                                                                | Что делает                                 |
| --------------------- | ---------------------------------------------------------------------- | ------------------------------------------ |
| Проверить файлы LFS   | `git lfs ls-files`                                                     | показывает список файлов в LFS             |
| Подготовить фильтр    | `echo "version https://git-lfs.github.com/spec/v1==>" > lfs-clean.txt` | создаёт правило для удаления ссылок на LFS |
| Очистить историю      | `git filter-repo --force --replace-text lfs-clean.txt`                 | удаляет LFS из всех коммитов               |
| Удалить мусор         | `rm -rf .git/lfs && git lfs uninstall`                                 | вычищает локальные данные                  |
| Переподключить remote | `git remote add origin <url>`                                          | добавляет чистый репозиторий               |
| Залить заново         | `git push -u origin main --force`                                      | пушит проект без LFS                       |
| Проверить             | `git lfs ls-files`                                                     | убеждаемся, что LFS исчез                  |

---

## 💾 Бонус: как делать локальные бэкапы перед такими операциями

Перед тем как переписывать историю (`git filter-repo`, `rebase`, `reset`),
сохраняй локальный архив:

```bash
git bundle create ../project-backup-$(date +%Y%m%d).bundle --all
```

А потом, если всё пойдёт не так:

```bash
git clone project-backup-20251109.bundle restored-repo
```
